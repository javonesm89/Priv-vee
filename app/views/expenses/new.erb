<% if @errors %>
  <div id="error_explanation">
    <h2><%= pluralize(@errors.count, "error") %> prohibited you from saving this expense:</h2>
    <ul>
      <% @errors.each.uniq do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>


ADD AN EXPENSE
<p>

<%= form_for(@planner) do |f| %>
    <%= f.fields_for :expense_attributes do |i| %> 
        SOURCE: <%= i.text_field :source, :placeholder => "Rent" %>
        AMOUNT: <%= i.number_field :amount, :placeholder => "1350" %>
    <% end %>
    <%= f.submit "Submit" %>   
<% end %>
<% if @errors %>
  <div id="error_explanation">
    <h2><%= pluralize(@errors.count, "error") %> prohibited you from saving this saving strategy:</h2>
    <ul>
      <% @errors.each.uniq do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>


ADD A SAVING STRATEGY
<p>

<%= form_for @planner do |f| %>
    <%= f.fields_for :saving_attributes do |i| %> 
        SOURCE: <%= i.text_field :source %>
        AMOUNT: <%= i.number_field :amount %>
    <% end %>
    <%= f.submit "Submit" %>   
<% end %>
<p>
<li>PRO TIP: for 401k, it's typical to invest what your employer matches (e.g, 6%). </li>
<li>PRO TIP: for a Safety Net, it's typical to save what between 5%-10% of your yearly income (e.g, 7% or $500). </li>

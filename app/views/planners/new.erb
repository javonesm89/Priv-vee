CREATE NEW PLANNER
<p>
<%= form_for(@planner) do |f| %>
    MONTH: <%= f.month_field :month %><p>
    <%= fields_for :income_attributes do |inc| %>
        INCOME: <%= inc.text_field :source %>
    <% end %>
    <%= f.submit "SUBMIT BUDGET PLANNER" %>
<% end %> 